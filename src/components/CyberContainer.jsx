export default function CyberContainer({ children }) {
  return (
    <main className="min-h-screen bg-[#0b0f1a] text-white relative">
      {/* global background accents */}
      {children}
    </main>
  );
}
